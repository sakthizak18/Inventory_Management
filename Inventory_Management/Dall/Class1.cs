using System;
using System.IO;
using System.Runtime.InteropServices.JavaScript;
using System.Text.Json;
using System.Text.Json.Nodes;
using System.Threading;

namespace Dall
{
    public class Class1
    {
       //static void main ()
       // {
       //     string filePath = "C:\\Projects_Github\\Inventory_Management\\Data.json";

       //      object _Read ()
       //     {
       //         try
       //         {
       //             // Read the entire JSON file as a string
       //             string jsonString = File.ReadAllText(filePath);

       //             // Deserialize the JSON string into a dynamic object
       //             dynamic jsonObject = JsonSerializer.Deserialize<dynamic>(jsonString);

       //             // Access specific properties of the JSON object
       //              {jsonObject["students"][0]["name"]};
       //              {jsonObject["students"][1]["name"]};
       //             foreach

       //             // Alternatively, you can use strongly typed classes for deserialization
       //             // For example, if you have a class named Student, you can deserialize like this:
       //             // Student[] students = JsonSerializer.Deserialize<Student[]>(jsonString);
       //         }
       //         catch (FileNotFoundException)
       //         {
       //             Console.WriteLine("File not found.");
       //         }
       //         catch (JsonException)
       //         {
       //             Console.WriteLine("Invalid JSON format.");
       //         }
       //         catch (Exception ex)
       //         {
       //             Console.WriteLine($"An error occurred: {ex.Message}");
       //         }
       //         return null ;
       //     }
        //}
    }
}